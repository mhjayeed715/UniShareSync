{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon start-clean.js",
    "server": "node server.js",
    "build": "npx prisma generate",
    "vercel-build": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "kill-port": "node -e \"require('child_process').exec('netstat -ano | findstr :5000', (e,o) => o && o.split('\\n').forEach(l => l.includes(':5500') && require('child_process').exec('taskkill /PID ' + l.trim().split(/\\s+/).pop() + ' /F')))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "bcryptjs": "^3.0.3",
    "cloudinary": "^1.41.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.0.0",
    "multer": "^2.0.2",
    "multer-storage-cloudinary": "^4.0.0",
    "node-fetch": "^2.7.0",
    "nodemailer": "^7.0.12",
    "pg": "^8.16.3",
    "prisma": "^5.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
